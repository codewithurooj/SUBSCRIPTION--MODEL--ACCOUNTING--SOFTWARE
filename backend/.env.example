# Postgres connection strings. In production this points at a Neon database;
# locally it can point at the docker-compose Postgres container.
DATABASE_URL=postgresql+psycopg://subscription:subscription_dev_pw@localhost:5433/subscription_model
DATABASE_URL_TEST=postgresql+psycopg://subscription:subscription_dev_pw@localhost:5433/subscription_model_test
